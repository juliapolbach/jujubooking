import React, { Component } from 'react'
import PropTypes from 'prop-types'
import { HeaderDiv } from '../styledComponents/header'

class Header extends Component {
  render () {
    return (
      <HeaderDiv>
      </HeaderDiv>
    )
  }
}

Header.propTypes = {}

export default Header
